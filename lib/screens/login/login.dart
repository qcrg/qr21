import 'package:flutter/material.dart';
import 'package:qr21/screens/login/rc_login.dart';

class Login extends StatelessWidget {
  const Login({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: RcLogin());
  }
}
